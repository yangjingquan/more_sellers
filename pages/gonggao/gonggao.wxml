<view class="index">
  <view class="recommend">
    <block wx:for="{{recommend_info}}">
      <view class="recommend_content" bindtap="getProDetail" data-proid="{{item.id}}">
        <view class="content">
          <text class="pro_name">{{item.title}}</text>
          <text class="time">{{item.create_time}}</text>
        </view>
      </view>
    </block>
  </view>
</view>